{
  "name": "refyn",
  "version": "0.1.0",
  "description": "Always-on prompt rewriter. Select text anywhere on Windows, hit a hotkey, get a better prompt in place.",
  "type": "module",
  "bin": { "refyn": "./refyn.mjs" },
  "scripts": {
    "start": "node refyn.mjs start",
    "daemon": "node daemon/server.mjs",
    "build": "node refyn.mjs build",
    "test": "node --test test/*.test.mjs"
  },
  "engines": { "node": ">=20.0.0" },
  "dependencies": {}
}
